on: [push, pull_request]
name: Test

env:
  DBNAME: testing.db

jobs:
#  test:
#    strategy:
#      matrix:
#        go-version: [1.17.x]
#        os: [ubuntu-latest, macos-latest]
#    runs-on: ${{ matrix.os }}
#    steps:
#      - name: Install Go
#        uses: actions/setup-go@v2
#        with:
#          go-version: ${{ matrix.go-version }}
#      - name: Checkout code
#        uses: actions/checkout@v2
#      - name: Test
#        run: go test ./... -v

  k6_load_test:
    name: k6 Load Test
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v1

      - name: Start containers
        run: |
          docker compose -f "docker-compose.yml" up -d --build

      - name: Install k6
        run: |
          curl https://github.com/loadimpact/k6/releases/download/v0.34.1/k6-v0.34.1-linux-amd64.tar.gz -L | tar xvz --strip-components 1

#      - name: Run local k6 load test
#        run: |
#          docker compose -f "docker-compose.yml" up -d
#          ./k6 run loadtest.js
      - name: Run k6
        uses: k6io/action@v0.2.0
        with:
          filename: loadtest.js

      - name: Stop containers
        if: always()
        run: docker compose -f "docker-compose.yml" down